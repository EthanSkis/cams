// Ski-resort webcams. Most resorts publish live cams on their own site with
// non-standard embedding. A comprehensive scrape requires per-resort adapters;
// we document the hook and leave coverage to future expansion.

export async function run() {
  return [];
}

export const info = { name: 'ski-resorts' };
